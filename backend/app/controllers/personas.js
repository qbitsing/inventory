'use strict';

class PersonasCtrl{
	listarAll (req , res) {
		res.send(`Se van a listar todas las personas`);
	}

	listarById (req , res) {
		res.send(`Se van a listar todas las personas`);
	}

	crear (req , res) {
		res.send(`Se van a listar todas las personas`);
	}

	actualizar (req , res) {
		res.send(`Se van a listar todas las personas`);
	}

	eliminar (req , res) {
		res.send(`Se van a listar todas las personas`);
	}
}

module.exports = new PersonasCtrl;